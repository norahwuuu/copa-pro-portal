apiVersion: v1
kind: Service
metadata:
  name: pro-portal
  namespace: aws-gateway
  labels:
    app: pro-portal
spec:
  type: ExternalName
  externalName: pro-portal.public.svc.cluster.local
  ports:
    - name: https
      protocol: TCP
      port: 8080